# Phase 12 Plan 03: Credential Storage & Validation

**Encrypted credential storage with AES-256-GCM and Anthropic API validation**

## Objective
Add encryption dependencies, create credential module, extend Settings, add async API validation.

## Tasks

### Task 1: Add new Cargo dependencies
- `type: auto`
- Add aes-gcm, base64, reqwest (rustls-tls, json), rand

### Task 2: Create credential encryption module
- `type: auto`
- src/config/credentials.rs — encrypt/decrypt with AES-256-GCM
- Derive key from machine_id + hostname
- Store as base64 in config

### Task 3: Extend Settings with credentials
- `type: auto`
- CredentialConfig struct in settings.rs
- Save/load with encrypted API key

### Task 4: Add async API validation
- `type: auto`
- Minimal Anthropic API client for key validation
- Wire to Ctrl+V in Access Portal
- Show result in validation status

## Verification
- cargo build succeeds
- Ctrl+V with valid key shows green status

## Success Criteria
- [ ] Credentials encrypted at rest in config.toml
- [ ] Decrypt on load works correctly
- [ ] API validation calls Anthropic endpoint
- [ ] Build succeeds
