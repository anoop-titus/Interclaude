# Phase 12 Plan 02: Access Portal — Page & Navigation

**New Access Portal tab between Setup and Bridge for Anthropic API credential management**

## Objective
Add Page::AccessPortal variant, create access_portal.rs TUI module with form layout, wire keybindings.

## Tasks

### Task 1: Add Page::AccessPortal and App state
- `type: auto`
- Add AccessPortal variant to Page enum
- Update next_page/prev_page: Setup → AccessPortal → Bridge
- Add enums: AccessMode (OAuth, ApiKey), ModelChoice (Sonnet46, Opus46, Haiku45), AccessPortalField (AccessMode, ApiKey, Model)
- Add fields to App: access_mode, api_key_input, model_selection, access_portal_field, credentials_saved
- Update page_tabs() to include Access Portal tab

### Task 2: Create access_portal.rs TUI module
- `type: auto`
- Form with progressive disclosure
- Sections: Authentication, Model
- Fields: access mode selector, API key (masked, shown only for ApiKey mode), model selector
- Status indicator for credential validation
- Nav bar with relevant keybindings

### Task 3: Wire keybindings and routing
- `type: auto`
- Add mod access_portal to tui/mod.rs
- Route drawing to access_portal::draw in draw()
- Add handle_access_portal_input() for Tab/Down/Up navigation, Enter cycling, char input
- Handle Esc (back to Setup)

## Verification
- cargo build succeeds
- All 4 tabs appear in status bar
- Navigation works forward/backward through all pages

## Success Criteria
- [ ] Page::AccessPortal renders between Setup and Bridge
- [ ] Progressive disclosure hides API key when OAuth selected
- [ ] Model selector cycles through 3 options
- [ ] Build succeeds
